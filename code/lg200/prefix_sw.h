// prefix_sw -- Sets the instruction-prefix state used by the following
// operation to the SW mode encoded by the immediate.
// Prefix-state effect only: the prefix is consumed by the following
// prefix-defined operation and no register value is produced.
return true;
