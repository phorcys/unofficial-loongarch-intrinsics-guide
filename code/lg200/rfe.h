// rfe -- Restores the exception return state from the scalar-register pair
// and transfers control by the encoded displacement.
// Execution-environment effect only: the scalar pair is consumed as the
// exception return state and no register result is produced.
return true;
