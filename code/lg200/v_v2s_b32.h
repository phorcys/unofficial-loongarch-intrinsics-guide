// v_v2s_b32 -- Copies the selected active lane's 32-bit vector source value to the scalar destination according to the vector-to-scalar selection operand
// The predicate gates the lane write; only the value result of the named operation is modeled here.
dst[0] = src[0];
