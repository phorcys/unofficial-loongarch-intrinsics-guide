// v_interp_2_f32 -- Completes the second 32-bit floating-point interpolation phase for an active fragment lane and writes the interpolated varying to the vector destination
// The predicate gates the lane write; only the value result of the named operation is modeled here.
dst[0] = src[0];
