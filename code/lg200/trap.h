// trap -- Raises the encoded shader trap and transfers control to the trap
// path.
// Execution-environment effect only: trap delivery requires the debug/trap
// interface to be enabled and carries process/queue attribution; no
// register value is produced.
return true;
