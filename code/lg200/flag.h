// flag -- Updates the encoded execution-domain flag state, including the VM
// selector when present.
// Execution-state effect only: the flag/VM state is carried in the execution
// domain and no register value is produced.
return true;
