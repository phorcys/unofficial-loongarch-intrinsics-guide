// b -- Adds the signed 32-bit displacement to the current control-flow position
// and continues execution at the resulting target.
// Control-flow effect only: the target is the control-flow position plus the
// signed 32-bit displacement; no register result is produced (oracle model:
// continuation/termination effect).
return true;
